#!/usr/bin/python3

# Loop through the ASCII values for lowercase letters (97 to 122)
for char in range(97, 123):
    print("{}".format(chr(char)), end='')

# Print a newline at the end (to match the expected behavior)
print()
